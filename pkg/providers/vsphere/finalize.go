package vsphere

import "github.com/netapp/cake/pkg/providers"

// Finalize handles saving deliverables and cleaning up the bootstrap VM
func (v *ProviderVsphere) Finalize(spec *providers.Spec) error {
	var err error

	return err
}
